# What challenges did you face when refactoring your code to use Bootstrap?
  it was hard to use tailwind because it's so new and it was challenging getting use to the difeerent classes and writing the sintax for it 
# How did using Bootstrap utility classes and components simplify your styling process?
    it only took a few lines of code to get the same result as a the previous lab 
# In what scenarios might you choose not to use Bootstrap and write custom CSS instead?
    if a project im working on would require more specific customization to change things that bootstrap has limits on
